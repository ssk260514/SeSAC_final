# Flutter 앱은 .env 파일을 직접 읽지 않습니다.
# 대신 run_app.ps1이 --dart-define 플래그로 아래 값을 자동 주입합니다.
#
# 사용 예시 (수동 실행 시):
#   flutter run --dart-define=API_BASE_URL=http://<서버_IP>:8000/api
#
# 자동 실행 (Wi-Fi IP 자동 감지):
#   ./run_app.ps1

# dart-define 변수 목록
# API_BASE_URL=http://<서버_IP>:8000/api
