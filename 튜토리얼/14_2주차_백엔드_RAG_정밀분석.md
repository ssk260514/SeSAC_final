# 14. 2주차 — 백엔드 RAG 정밀 분석 (Grad-CAM + 임베딩 + LLM)

> **이 단계가 끝나면**: `POST /api/inspect`가 더미 응답이 아니라 실제 PyTorch 모델 재추론 + Grad-CAM 히트맵 PNG + BGE-M3 임베딩 + pgvector 검색 + LLM 조치 가이드를 반환합니다.
>
> **예상 시간**: 6시간 (모델 다운로드 시간 포함)
>
> **중요도**: ★★★ — AI 핵심이지만 복잡합니다. 학습 팀이 모델을 제공하지 않았다면 외부 LLM API로 우회하는 단계적 접근을 추천합니다.

> **참조 명세서**: `아키텍처_명세서.md` RAG 파이프라인, `API_SPEC.md` INFER-002, `schema_diagram.md` 매뉴얼/조치_권고/조치_권고_매뉴얼

---

## 0. 단계적 접근 — 무엇을 먼저 할 것인가

이 단계는 4개 큰 컴포넌트를 합칩니다. 한 번에 다 만들면 디버깅이 지옥이므로 **순서대로** 진행하세요.

| 순서 | 컴포넌트 | 검증 방법 |
|---|---|---|
| 14-A | PyTorch 모델 로드 + 분류 | 사진 → Top-3 응답 |
| 14-B | Grad-CAM 히트맵 생성 | PNG 파일 생성·반환 |
| 14-C | 매뉴얼 시드 + BGE-M3 임베딩 | pgvector 검색이 매뉴얼 청크 반환 |
| 14-D | LLM 조치 가이드 생성 | 자연어 조치 텍스트 반환 |
| 14-E | 모든 조각 통합 → INFER-002 본체 갈아끼우기 | Flutter 모달에서 진짜 데이터 |

### LLM 선택 — 로컬 GPU 없을 때

운영 명세는 `Qwen 2.5 7B AWQ + vLLM`이지만, **로컬 GPU(NVIDIA T4 이상)가 없으면** 외부 API로 우회하세요:

- **Anthropic Claude API** (권장): `claude-haiku-4-5` — 빠르고 저렴
- **OpenAI API**: `gpt-4o-mini`
- **OpenRouter**: 여러 모델 한 번에

이 매뉴얼은 **Anthropic API**로 작성하지만, OpenAI도 호환되도록 `chat completion` 패턴을 따릅니다.

---

## 14-A. PyTorch 모델 로드 + 분류

### A-1. 의존성 추가

`backend/requirements.txt`에 추가:

```txt
torch==2.6.0
torchvision==0.21.0
pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
```

> ⚠️ torch는 1GB가 넘습니다. 첫 설치 시 10~20분.
> ⚠️ torch < 2.6은 CVE-2025-32434 보안 취약점으로 `torch.load` 자체가 거부됨 — 반드시 2.6.0 이상 사용.
> ⚠️ `.venv` 가상환경 없음 — 시스템 Python 3.12에 직접 설치.

설치:
```powershell
cd C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
C:\Users\yejin\AppData\Local\Programs\Python\Python312\Scripts\pip.exe install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
C:\Users\yejin\AppData\Local\Programs\Python\Python312\Scripts\pip.exe install -r requirements.txt
```

### A-2. 모델 파일

`backend/models/best_model_v5_datamatch_full.pth`에 학습 팀 제공 PyTorch 가중치를 두세요.

없다면 torchvision의 사전학습 MobileNetV3을 fine-tuning 없이 임시 사용:

```python
# backend/scripts/init_dummy_model.py
import torch
import torchvision.models as models
import os

os.makedirs("models", exist_ok=True)
model = models.mobilenet_v3_large(weights=models.MobileNet_V3_Large_Weights.IMAGENET1K_V2)
# 마지막 분류 레이어를 7클래스로 교체 (학습 안 됨 - 더미)
model.classifier[3] = torch.nn.Linear(in_features=1280, out_features=30)  # 단일 통합 모델 30클래스
torch.save(model.state_dict(), "models/best_model_v5_datamatch_full.pth")
print("Dummy model saved")
```

```powershell
python scripts/init_dummy_model.py
```

### A-3. 분류 서비스

`backend/app/services/classifier.py`:

```python
import torch
import torchvision.models as models
import torchvision.transforms as T
from PIL import Image, ImageOps
import io


_CLASSES = [
    # 표면처리 (0~12)
    "균열-도장", "균열-보온재", "도장흐름-도장", "도막떨어짐-도장", "도막분리-도장",
    "스크래치-모재", "스크래치-도장", "스크래치-보온재", "보온재손상-보온재", "탱크클리닝불량-모재",
    "표면양품-모재", "표면양품-도장", "표면양품-보온재",
    # 용접 (13~15)
    "용접불량-조인트", "용접블로우홀-조인트", "용접양품-조인트",
    # 절단 (16~19)
    "절단불량-모재", "절단불량-보온재", "절단양품-모재", "절단양품-보온재",
    # 케이블 (20~25)
    "케이블설치불량-케이블그랜드", "케이블손상-케이블", "바인딩불량-케이블타이",
    "케이블설치양품-케이블그랜드", "케이블양품-케이블", "바인딩양품-케이블타이",
    # 파이프 (26~27)
    "볼트체결불량-파이프", "볼트체결양품-파이프",
    # 폼스프레이 (28~29)
    "폼스프레이불량-우레탄폼", "폼스프레이양품-우레탄폼",
]  # 단일 통합 모델 30클래스 (불량 19 + 양품 11)


_transform = T.Compose([
    T.Resize((384, 384)),   # 실제 모델 입력 크기 384×384 (224×224 아님)
    T.ToTensor(),
    T.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),
])


class UnifiedClassifier:
    def __init__(self, weights_path: str, device: str = "cpu"):
        self.device = device
        self.model = models.mobilenet_v3_large(weights=None)
        self.model.classifier[3] = torch.nn.Linear(in_features=1280, out_features=len(_CLASSES))
        self.model.load_state_dict(torch.load(weights_path, map_location=device, weights_only=True))
        self.model.eval()
        self.model.to(device)

    def predict(self, image_bytes: bytes) -> dict:
        img = ImageOps.exif_transpose(Image.open(io.BytesIO(image_bytes)).convert("RGB"))
        tensor = _transform(img).unsqueeze(0).to(self.device)
        with torch.no_grad():
            logits = self.model(tensor)
            probs = torch.softmax(logits, dim=1)[0].cpu().numpy()

        top3_idx = probs.argsort()[-3:][::-1]
        top3 = [{"class": _CLASSES[i], "confidence": float(probs[i])} for i in top3_idx]
        return {
            "defect_type": top3[0]["class"],
            "confidence": top3[0]["confidence"],
            "top3": top3,
            "is_defect": "양품" not in top3[0]["class"],
        }


_classifier_singleton: UnifiedClassifier | None = None


def get_classifier() -> UnifiedClassifier:
    global _classifier_singleton
    if _classifier_singleton is None:
        _classifier_singleton = UnifiedClassifier("models/best_model_v5_datamatch_full.pth")
    return _classifier_singleton
```

---

## 14-B. Grad-CAM 히트맵

### B-1. 의존성

```txt
grad-cam==1.5.4
```

```powershell
pip install grad-cam==1.5.4
```

### B-2. 서비스

`backend/app/services/gradcam.py`:

```python
import io
import os
import numpy as np
import torch
import torchvision.transforms as T
from PIL import Image, ImageOps
from pytorch_grad_cam import GradCAM
from pytorch_grad_cam.utils.image import show_cam_on_image

from .classifier import get_classifier


_transform = T.Compose([
    T.Resize((384, 384)),   # 실제 모델 입력 크기 384×384 (224×224 아님)
    T.ToTensor(),
    T.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),
])


def generate_heatmap(image_bytes: bytes, target_class: int, save_dir: str = "uploads/heatmaps") -> str:
    """결함 클래스에 대한 Grad-CAM 히트맵 PNG를 생성하고 로컬 경로 반환."""
    os.makedirs(save_dir, exist_ok=True)

    clf = get_classifier()
    # MobileNetV3-Large의 마지막 컨볼루션 블록 (features의 마지막)
    target_layer = clf.model.features[-1]

    img = ImageOps.exif_transpose(Image.open(io.BytesIO(image_bytes)).convert("RGB"))
    rgb_arr = np.array(img.resize((384, 384))) / 255.0   # 실제 모델 입력 크기 384×384 (224×224 아님)
    tensor = _transform(img).unsqueeze(0).to(clf.device)

    cam = GradCAM(model=clf.model, target_layers=[target_layer])
    targets = [__import__("pytorch_grad_cam").utils.model_targets.ClassifierOutputTarget(target_class)]
    grayscale_cam = cam(input_tensor=tensor, targets=targets)[0]

    overlay = show_cam_on_image(rgb_arr.astype(np.float32), grayscale_cam, use_rgb=True)
    out_path = os.path.join(save_dir, f"heatmap_{__import__('uuid').uuid4().hex}.png")
    Image.fromarray(overlay).save(out_path)
    return f"local://{out_path}"   # MVP: 로컬 경로. 운영은 S3 URL.
```

> 💡 운영에서는 `boto3`로 S3 업로드 후 presigned URL을 반환. MVP는 로컬 디스크 경로로 시작합니다.

### B-3. 정적 파일 서빙 (Flutter에서 이미지 다운로드 가능하도록)

`app/main.py`에 추가:

```python
from fastapi.staticfiles import StaticFiles
import os

os.makedirs("uploads", exist_ok=True)
app.mount("/uploads", StaticFiles(directory="uploads"), name="uploads")
```

이렇게 하면 `http://localhost:8000/uploads/heatmaps/heatmap_xxx.png` 로 접근 가능.

`generate_heatmap`이 반환하는 `local://uploads/heatmaps/...` 경로를 Flutter에 줄 때 다음으로 변환:
```python
def to_public_url(local_path: str, base_url: str = "http://172.16.210.34:8000") -> str:
    # "local://uploads/..." → "http://.../uploads/..."
    # 에뮬레이터: http://10.0.2.2:8000 / 실제 기기: PC의 LAN IP (예: 172.16.210.34:8000)
    return local_path.replace("local://", f"{base_url}/")
```

---

## 14-C. 매뉴얼 시드 + BGE-M3 임베딩

### C-1. 의존성

```txt
sentence-transformers==3.0.1
tf-keras
```

```powershell
pip install sentence-transformers==3.0.1
pip install tf-keras
```

> ⚠️ BGE-M3는 약 2.3GB. 처음 로드 시 다운로드.
> ⚠️ 시스템에 Keras 3이 설치된 경우 `tf-keras` 추가 설치 필요. 없으면 `sentence_transformers` 임포트 시 `ValueError: Keras 3` 오류 발생.

### C-2. 매뉴얼 청크 시드 데이터

`backend/scripts/seed_manuals.py`:

> 6개 공정 전체(용접/표면처리/파이프/케이블/절단/폼스프레이) 30개 청크, 실제 KGS/KR 규정 근거 포함.
> 튜토리얼 더미 5개 청크에서 실제 데이터로 교체. `PROCESS_ID` 딕셔너리로 공정명 → DB ID 변환.

```python
# backend/scripts/seed_manuals.py
import asyncio, os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from sqlalchemy import text
from sentence_transformers import SentenceTransformer
from app.core.db import engine

# DB 공정 테이블의 실제 ID로 수정 필요 (SELECT * FROM 공정;)
PROCESS_ID = {
    "표면처리": 1,
    "용접":     2,
    "절단":     3,
    "케이블":   4,
    "파이프":   5,
    "폼스프레이": 6,
}

_CHUNKS = [
    # 각 공정별 실제 KGS/KR 규정 기반 청크 30개
    # (파일 전체 내용은 backend/scripts/seed_manuals.py 참조)
]


async def main():
    model = SentenceTransformer("BAAI/bge-m3")
    print("BGE-M3 loaded")

    async with engine.connect() as conn:
        await conn.execute(text("DELETE FROM 조치_권고_매뉴얼"))
        await conn.execute(text("DELETE FROM 매뉴얼"))

        for c in _CHUNKS:
            pid = PROCESS_ID[c["process"]]
            vec = model.encode(c["content"], normalize_embeddings=True).tolist()
            await conn.execute(text("""
                INSERT INTO 매뉴얼 (공정_ID, 제목, 내용, 내용_벡터, 출처, 페이지_번호, 청크_순서)
                VALUES (:pid, :title, :content, CAST(:vec AS vector), :source, :page, :order)
            """), {
                "pid": pid, "title": c["title"], "content": c["content"],
                "vec": str(vec), "source": c["source"],
                "page": c["page"], "order": c["chunk_order"],
            })

        await conn.commit()
    print(f"Inserted {len(_CHUNKS)} manual chunks")


if __name__ == "__main__":
    asyncio.run(main())
```

실행:
```powershell
python scripts/seed_manuals.py
```

### C-3. 검색 서비스

`backend/app/services/manual_search.py`:

```python
from sentence_transformers import SentenceTransformer
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import text


_model_singleton: SentenceTransformer | None = None


def get_embedder() -> SentenceTransformer:
    global _model_singleton
    if _model_singleton is None:
        _model_singleton = SentenceTransformer("BAAI/bge-m3")
    return _model_singleton


async def search_manuals(db: AsyncSession, query_text: str, process_id: int, top_k: int = 3):
    vec = get_embedder().encode(query_text, normalize_embeddings=True).tolist()
    res = await db.execute(text("""
        SELECT 매뉴얼_ID, 제목, 내용, 출처, 페이지_번호, 청크_순서,
               1 - (내용_벡터 <=> CAST(:vec AS vector)) AS similarity
        FROM 매뉴얼
        WHERE 공정_ID = :pid AND 내용_벡터 IS NOT NULL
        ORDER BY 내용_벡터 <=> CAST(:vec AS vector)
        LIMIT :k
    """), {"vec": str(vec), "pid": process_id, "k": top_k})
    return [
        {
            "manual_id": r[0],
            "title": r[1],
            "content": r[2],
            "source": r[3],
            "page": r[4],
            "chunk_order": r[5],
            "similarity": float(r[6]),
        }
        for r in res.all()
    ]
```

> 💡 **pgvector 연산자**: `<=>` 는 cosine distance (0~2). similarity = 1 - distance (정규화된 벡터 가정).

---

## 14-D. LLM 조치 가이드 생성

### D-1. 의존성

```txt
openai==2.38.0
```

```powershell
pip install openai==2.38.0
```

> ⚠️ openai 1.x는 httpx 0.28.x와 충돌 (`AsyncHttpxClientWrapper._state` 오류) — 반드시 2.38.0 이상 사용.

`.env`에 추가:
```env
OPENAI_API_KEY=sk-proj-...
```

`backend/app/core/config.py`의 `Settings`에 추가:
```python
    OPENAI_API_KEY: str = ""
```

### D-2. 서비스

`backend/app/services/llm.py`:

```python
import json
from openai import AsyncOpenAI
from app.core.config import settings


_client_singleton: AsyncOpenAI | None = None


def get_llm_client() -> AsyncOpenAI:
    global _client_singleton
    if _client_singleton is None:
        _client_singleton = AsyncOpenAI(api_key=settings.OPENAI_API_KEY)
    return _client_singleton


async def generate_action_guide(defect_type: str, manuals: list[dict]) -> dict:
    manuals_text = "\n\n".join([
        f"[매뉴얼 {i+1} - {m['title']}, p.{m['page']}]\n{m['content']}"
        for i, m in enumerate(manuals)
    ])

    prompt = f"""당신은 선박 LNG 탱크 검사 보조 AI입니다. 검사원이 발견한 결함에 대한 조치 가이드를 생성합니다.

발견된 결함: {defect_type}

참고 매뉴얼:
{manuals_text}

위 매뉴얼을 종합하여 다음 JSON 형식으로 출력하세요. 매뉴얼에 없는 내용은 추측하지 말고, 매뉴얼의 절차를 충실히 따르세요.

{{
  "summary": "한 줄 요약 (50자 이내)",
  "detail": "단계별 상세 조치 (번호 매김, 4~6단계, 각 단계는 짧고 명확하게)"
}}

JSON 외 다른 텍스트는 절대 출력하지 마세요."""

    res = await get_llm_client().chat.completions.create(
        model="gpt-4o-mini",
        max_tokens=1024,
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
    )
    raw = res.choices[0].message.content.strip()
    guide = json.loads(raw)
    # OpenAI가 detail을 list로 반환하는 경우 대비
    if isinstance(guide.get("detail"), list):
        guide["detail"] = "\n".join(guide["detail"])
    return guide
```

> ⚠️ **Anthropic을 쓰는 경우**: `from anthropic import AsyncAnthropic`으로 교체. `messages.create` 사용, `response_format` 미지원이므로 코드 펜스 제거 로직 필요.
> ⚠️ **로컬 vLLM을 쓰는 경우**: `AsyncOpenAI(base_url="http://localhost:8001/v1")`로 vLLM 서버 호환 모드.

---

## 14-E. 통합 — INFER-002 본체 갈아끼우기

`backend/app/api/inspect.py`의 `inspect` 함수를 다음으로 교체:

> ⚠️ 파일 상단 import에 `import json`, `import os` 추가 필요.
> ⚠️ `sector`, `subsector` 파라미터는 이전 튜토리얼에서 추가된 것으로 유지.
> ⚠️ classifier의 `_CLASSES`는 모듈 레벨 `_CLASSES`와 이름 충돌을 피해 `_CLF_CLASSES`로 alias.

```python
@router.post("/inspect")
async def inspect(
    image: UploadFile = File(...),
    session_id: int = Form(...),
    process_id: int = Form(...),
    tank_type: str = Form(...),
    sector: str | None = Form(default=None),
    subsector: str | None = Form(default=None),
    on_device_result: str | None = Form(default=None),
    inspector_id: int = Depends(get_current_inspector_id),
    db: AsyncSession = Depends(get_db),
):
    # 1) 이미지 읽기
    image_bytes = await image.read()

    # 2) PyTorch 분류
    from app.services.classifier import get_classifier, _CLASSES as _CLF_CLASSES
    clf_result = get_classifier().predict(image_bytes)
    top1_class = clf_result["defect_type"]
    top1_idx = _CLF_CLASSES.index(top1_class)
    confidence = clf_result["confidence"]
    is_defect_flag = clf_result["is_defect"]

    # 3) Grad-CAM (항상 생성)
    from app.services.gradcam import generate_heatmap
    local = generate_heatmap(image_bytes, target_class=top1_idx)
    heatmap_url = local.replace("local://", "http://172.16.210.34:8000/")
    # 에뮬레이터: http://10.0.2.2:8000 / 실제 기기: PC의 LAN IP

    # 4) 이미지 저장 (로컬)
    os.makedirs("uploads/images", exist_ok=True)
    file_uuid = uuid.uuid4().hex
    img_path = f"uploads/images/{file_uuid}.jpg"
    with open(img_path, "wb") as f:
        f.write(image_bytes)
    image_url = f"http://172.16.210.34:8000/{img_path}"
    # 에뮬레이터: http://10.0.2.2:8000 / 실제 기기: PC의 LAN IP

    # 5) 검사_이미지 INSERT
    img_row = (await db.execute(text("""
        INSERT INTO 검사_이미지 (세션_ID, 이미지_경로, 촬영_일시)
        VALUES (:sid, :path, NOW()) RETURNING 이미지_ID
    """), {"sid": session_id, "path": image_url})).first()
    image_id = img_row[0]

    # 서버 결과 INSERT
    server_res = (await db.execute(text("""
        INSERT INTO 검사_결과 (이미지_ID, 공정_ID, 추론_위치, 대표_여부, 품질_여부,
            결함_유형, 신뢰도_점수, 상위_예측, 추론_지연_ms,
            사람_재확인_필요, "Grad-CAM_경로", 결과_처리_상태)
        VALUES (:iid, :pid, '서버', true, :ok, :dtype, :conf, CAST(:top3 AS JSONB), :ms,
                :review, :gradcam, '미완료')
        RETURNING 결과_ID
    """), {
        "iid": image_id, "pid": process_id, "ok": not is_defect_flag,
        "dtype": top1_class, "conf": confidence,
        "top3": json.dumps(clf_result["top3"]),
        "ms": 1200, "review": confidence < 0.70,
        "gradcam": heatmap_url,
    })).first()
    server_result_id = server_res[0]

    # 단말 결과 동봉 시 row 추가 (대표=false)
    if on_device_result:
        try:
            dev = json.loads(on_device_result)
            await db.execute(text("""
                INSERT INTO 검사_결과 (이미지_ID, 공정_ID, 추론_위치, 대표_여부, 품질_여부,
                    결함_유형, 신뢰도_점수, 상위_예측, 추론_지연_ms, 결과_처리_상태)
                VALUES (:iid, :pid, '단말', false, :ok, :dtype, :conf, CAST(:top3 AS JSONB), :ms, '미완료')
            """), {
                "iid": image_id, "pid": process_id,
                "ok": "양품" not in dev["defect_type"],
                "dtype": dev["defect_type"], "conf": dev["confidence"],
                "top3": json.dumps(dev.get("top3_predictions", [])),
                "ms": dev.get("inference_ms", 0),
            })
        except Exception:
            pass

    # 세션 카운터
    await db.execute(text("""
        UPDATE 검사_세션 SET 총_이미지_수 = 총_이미지_수 + 1,
                          양품_수 = 양품_수 + :p,
                          불량_수 = 불량_수 + :d
        WHERE 세션_ID = :sid
    """), {"sid": session_id, "p": 0 if is_defect_flag else 1, "d": 1 if is_defect_flag else 0})

    # 6) RAG: 불량인 경우 매뉴얼 검색 + LLM 조치 가이드
    action_data = None
    if is_defect_flag:
        from app.services.manual_search import search_manuals
        from app.services.llm import generate_action_guide

        print(f"[RAG] 불량 감지: {top1_class}, process_id={process_id}")
        manuals = await search_manuals(db, top1_class, process_id, top_k=3)
        print(f"[RAG] 매뉴얼 검색 결과: {len(manuals)}건")
        if manuals:
            try:
                print("[RAG] LLM 호출 시작")
                guide = await generate_action_guide(top1_class, manuals)
                print(f"[RAG] LLM 응답: {guide}")
                rec_row = (await db.execute(text("""
                    INSERT INTO 조치_권고 (결과_ID, 조치_요약, 조치_상세, 생성_일시, 수정_일시)
                    VALUES (:rid, :sum, :det, NOW(), NOW())
                    RETURNING 권고_ID
                """), {"rid": server_result_id, "sum": guide["summary"], "det": guide["detail"]})).first()
                rec_id = rec_row[0]

                for i, m in enumerate(manuals):
                    await db.execute(text("""
                        INSERT INTO 조치_권고_매뉴얼 (권고_ID, 매뉴얼_ID, 순위, 유사도_점수)
                        VALUES (:r, :m, :rank, :sim)
                    """), {"r": rec_id, "m": m["manual_id"], "rank": i+1, "sim": m["similarity"]})

                action_data = {
                    "recommendation_id": rec_id,
                    "summary": guide["summary"],
                    "detail": guide["detail"],
                    "source_manuals": [
                        {"manual_id": m["manual_id"], "title": m["title"], "page": m["page"],
                         "chunk_order": m["chunk_order"], "rank": i+1, "similarity": m["similarity"]}
                        for i, m in enumerate(manuals)
                    ],
                }
            except Exception as e:
                action_data = {
                    "recommendation_id": None,
                    "summary": f"RAG 실패: {e}",
                    "detail": "매뉴얼 검색 또는 LLM 호출에 실패했습니다.",
                    "source_manuals": [],
                }

    await db.commit()

    return {
        "image_id": image_id,
        "server_result": {
            "result_id": server_result_id,
            "defect_type": top1_class,
            "confidence": confidence,
            "inference_ms": 1200,
            "needs_human_review": confidence < 0.70,
            "top3_predictions": clf_result["top3"],
        },
        "heatmap_url": heatmap_url,
        "action_guide": action_data or {
            "recommendation_id": None,
            "summary": "양품 — 조치 불필요",
            "detail": "",
            "source_manuals": [],
        },
    }
```

---

## 14-F. pgvector 인덱스 (행이 100개 이상이면)

매뉴얼 청크가 충분히 쌓이면 검색 속도를 위해 인덱스를 추가합니다 (현재 5개라 불필요하지만 운영 단계에서):

```sql
CREATE INDEX idx_manual_vector ON 매뉴얼 USING ivfflat (내용_벡터 vector_cosine_ops) WITH (lists = 100);
```

---

## 동작 확인

### 시나리오 — 진짜 정밀 분석

1. 백엔드 재시작 (`uvicorn ...`)
2. 첫 요청 시 BGE-M3 + PyTorch 모델 로드로 30초 정도 걸림. 이후 빠름.
3. Flutter 앱에서 불량 사진 촬영
4. 검사 이력 → 양품/불량품 이력 카드 → 정밀 분석 모달
5. **이번엔 진짜 데이터**:
   - 이미지 URL이 실제 이미지를 가리킴 (Network Image 위젯 사용)
   - Grad-CAM URL이 진짜 PNG 파일
   - Top-3 예측이 진짜 PyTorch 결과
   - RAG 조치 가이드가 매뉴얼 기반 자연어 텍스트
   - 매뉴얼 출처 3건이 진짜 매뉴얼 청크
6. DB 확인:
   ```sql
   SELECT * FROM 조치_권고 ORDER BY 권고_ID DESC LIMIT 1;
   SELECT * FROM 조치_권고_매뉴얼 ORDER BY 매핑_ID DESC LIMIT 3;
   ```

### Flutter 측 이미지 표시 — `cached_network_image`로 교체

10번 단계의 모달은 placeholder 텍스트만 표시했습니다. 이제 진짜 이미지를 표시하도록 갱신:

`features/inspection_history/presentation/widgets/detail_modal.dart`의 이미지 영역을 다음과 같이:

```dart
import 'package:cached_network_image/cached_network_image.dart';

// _content 메서드의 AspectRatio child를:
Container(
  clipBehavior: Clip.hardEdge,
  decoration: BoxDecoration(
    color: AppColors.surfaceVariant,
    borderRadius: BorderRadius.circular(12),
  ),
  child: Stack(
    children: [
      CachedNetworkImage(
        imageUrl: _gradcamOn && (image['gradcam_url'] != null)
            ? image['gradcam_url']
            : image['image_url'],
        fit: BoxFit.cover,
        width: double.infinity,
        height: double.infinity,
        placeholder: (_, __) => const Center(child: CircularProgressIndicator()),
        errorWidget: (_, __, ___) => const Center(child: Icon(Icons.broken_image)),
      ),
      // ... Grad-CAM 토글 (기존 그대로)
    ],
  ),
),
```

매뉴얼 출처 리스트에서 유사도 % 표시는 제거합니다 (`detail_modal.dart`의 `ListTile` trailing 제거):

```dart
ListTile(
  dense: true,
  leading: const Icon(Icons.description, size: 18),
  title: Text('${mm['title']} - p.${mm['page']}', style: AppTextStyles.bodyMd),
  // trailing: Text(...) 제거 — 유사도 % 미표시
),
```

---

## 튜토리얼 외 추가 수정 사항

### TFLite 파일 확장자 오류

파일 저장 시 `.tflite.tfliet`처럼 이중 확장자로 저장되는 경우가 있음 → 수동으로 `.tflite`로 rename.

### TFLite 오류 시 서버 fallback (capture_repository_impl.dart)

TFLite 로드 실패 시 앱 크래시 방지를 위해 try-catch로 감싸고 서버 추론으로 fallback:

```dart
TfliteInferenceResult? local;
try {
  local = await tflite.infer(imageFile);
} catch (_) {
  local = null;
}
final onDeviceJson = local == null ? null : jsonEncode({...});
```

`capture_remote_data_source.dart`에서도 `on_device_result`를 nullable로 처리:

```dart
Future<InspectResponseDto> uploadAndInspectWithDevice({
  ...
  String? onDeviceJson,  // nullable
}) async {
  final formMap = <String, dynamic>{...};
  if (onDeviceJson != null) formMap['on_device_result'] = onDeviceJson;
  ...
}
```

### result.py 더미 텍스트 제거

이전 단계에서 남겨둔 `[MVP 더미] 14번 단계에서 RAG로 생성됩니다.` 텍스트를 실제 데이터로 교체:

```python
"action_guide": action or {
    "recommendation_id": None,
    "summary": "양품 — 조치 불필요",
    "detail": "",
    "source_manuals": [],
},
```

---

## 자주 발생하는 오류와 해결

### "RuntimeError: CUDA out of memory"
- GPU에 메모리 부족. `UnifiedClassifier(weights_path, device="cpu")`로 CPU 강제
- 또는 GPU 인스턴스를 더 큰 것으로 (g5.xlarge)

### "Cannot connect to BGE-M3" — 모델 다운로드 실패
- `~/.cache/huggingface/` 에 다운로드됨. 네트워크 확인. 회사망에서 막힐 경우 미리 다운로드 후 마운트

### OpenAI API "Invalid API key"
- `.env`의 `OPENAI_API_KEY`가 비어 있거나 잘못됨
- `platform.openai.com`에서 키 발급

### LLM 응답이 JSON이 아닌 일반 텍스트
- 프롬프트에 "JSON 외 다른 텍스트는 절대 출력하지 마세요" 강조됐는지 확인
- Claude는 보통 잘 따르지만, 가끔 ```json 펜스를 붙임 → 우리 코드가 이미 제거

### 매뉴얼 검색이 의미 없는 결과
- 매뉴얼 청크가 5개뿐. 운영에서는 100건 이상 필요
- 청크 길이가 너무 짧으면 임베딩이 부정확. 200~500자 권장

### Flutter에서 이미지가 안 보임
- `image_url`이 `localhost`가 아닌 `10.0.2.2`로 가야 함 (위 코드의 `to_public_url`)
- 폰에서는 PC의 LAN IP로 변환

---

## ✅ 다음 단계로 가기 전 체크리스트

- [ ] `POST /api/inspect`가 더미가 아닌 진짜 PyTorch 결과를 반환한다
- [ ] 불량 응답에 `heatmap_url`이 실제 PNG 파일을 가리킨다
- [ ] 브라우저에서 `http://10.0.2.2:8000/uploads/heatmaps/heatmap_xxx.png` 가 직접 보인다
- [ ] 매뉴얼 청크가 DB에 5개 이상 들어 있고 임베딩 벡터가 NULL이 아니다
- [ ] RAG 응답에 `source_manuals`가 3건 들어 있고 `similarity`가 0~1 사이 값이다
- [ ] LLM 응답 `summary`와 `detail`이 매뉴얼 내용에 근거한 자연어다
- [ ] Flutter 모달에서 원본 이미지 + Grad-CAM 토글 + 진짜 Top-3 + 진짜 RAG가 모두 보인다

다음 단계 **[15_빌드_배포_및_트러블슈팅.md](15_빌드_배포_및_트러블슈팅.md)** 로 이동하세요. 2주차 마무리이며, 이어서 3·4주차(16~20)로 이어집니다.
