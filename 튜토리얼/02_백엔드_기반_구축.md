# 02. 백엔드 기반 구축 — PostgreSQL + FastAPI + 12 테이블

> **이 단계가 끝나면**: Docker로 PostgreSQL+pgvector가 띄워져 있고, FastAPI가 8000번 포트에서 `/api/health`로 응답하며, 12개 테이블이 모두 생성되어 시드 데이터가 들어 있는 상태가 됩니다.
>
> **예상 시간**: 3시간

> 💡 **이 단계는 백엔드만 다룹니다.** Flutter 앱과의 연동은 05번부터 시작합니다. 백엔드 단독으로 동작하는 것을 확인합니다.

> **참조 명세서**: `schema_diagram.md` (12 테이블 정의), `API_SPEC.md` (엔드포인트 구조), `아키텍처_명세서.md` (배포 구조)

---

## 1. 백엔드 폴더 구조 만들기

PowerShell에서:

```powershell
mkdir C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
cd C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
```

다음 폴더 구조를 만듭니다:

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              ← FastAPI 진입점
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py        ← 환경 변수 로드
│   │   ├── security.py      ← bcrypt + JWT
│   │   └── db.py            ← SQLAlchemy 세션
│   ├── api/
│   │   ├── __init__.py
│   │   ├── deps.py          ← 의존성 (current_user 등)
│   │   ├── auth.py          ← 인증 라우터 (05번에서 채움)
│   │   ├── tank.py          ← 탱크/공정 (07번에서 채움)
│   │   ├── session.py       ← 세션 (07·08번)
│   │   ├── inspect.py       ← 추론 (09·14번)
│   │   ├── result.py        ← 결과·피드백 (10·11번)
│   │   └── settings.py      ← 설정 (12번)
│   ├── models/
│   │   ├── __init__.py
│   │   └── orm.py           ← SQLAlchemy 모델 (12 테이블)
│   └── schemas/
│       ├── __init__.py
│       └── auth.py          ← Pydantic 스키마 (05번부터 채움)
├── sql/
│   ├── 001_schema.sql       ← 12 테이블 DDL
│   └── 002_seed.sql         ← 시드 데이터
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── .env                     ← 환경 변수 (절대 git에 올리지 말 것)
```

PowerShell로 한 번에 만들기:

```powershell
mkdir app, app\core, app\api, app\models, app\schemas, sql
New-Item app\__init__.py, app\core\__init__.py, app\api\__init__.py, app\models\__init__.py, app\schemas\__init__.py -ItemType File
```

---

## 2. requirements.txt

`backend\requirements.txt` 파일을 만들고 다음 내용 저장:

```txt
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.10
python-dotenv==1.0.1
pgvector==0.3.4
```

> 💡 **버전을 고정**하는 이유: 미래에 라이브러리 업데이트로 갑자기 안 돌아가는 일을 막기 위함. 처음 배우는 단계에서는 "내 환경 = 매뉴얼 환경" 일치가 핵심.

---

## 3. 가상환경(venv) + 의존성 설치

```powershell
cd C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

PowerShell 프롬프트 왼쪽에 `(.venv)`가 붙으면 가상환경 활성화 성공.

> ⚠️ **PowerShell 실행 정책 오류**가 나면:
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```
> 한 번 실행 후 다시 시도.

가상환경 안에서 의존성 설치:

```powershell
pip install --upgrade pip
pip install -r requirements.txt
```

5분 정도 걸립니다.

> 💡 **venv가 뭔가요?** 프로젝트별로 격리된 Python 환경입니다. 시스템 전역 pip을 더럽히지 않고, 다른 프로젝트의 버전 충돌을 피할 수 있습니다. **다음에 작업할 때마다 `.\.venv\Scripts\Activate.ps1`를 먼저 실행**해야 합니다.

---

## 4. docker-compose.yml (PostgreSQL + pgvector)

`backend\docker-compose.yml`:

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: inspection_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: inspection_db
      POSTGRES_USER: inspection_user
      POSTGRES_PASSWORD: inspection_pass_change_me
      TZ: Asia/Seoul
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inspection_user -d inspection_db"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
```

핵심 포인트:
- `pgvector/pgvector:pg16` 이미지를 쓰면 PostgreSQL 16 + pgvector 확장이 함께 설치된 상태로 시작
- `./sql:/docker-entrypoint-initdb.d` — 컨테이너 첫 시작 시 `sql/` 폴더의 `.sql` 파일들을 알파벳 순으로 자동 실행 (001_schema.sql → 002_seed.sql)
- 데이터는 `postgres_data` 볼륨에 영속화 → 컨테이너 지워도 데이터는 남음

> ⚠️ `POSTGRES_PASSWORD`는 학습용 더미 값입니다. 실제 운영에는 강한 비밀번호를 `.env`로 주입하세요.

---

## 5. .env 파일

`backend\.env`:

```env
# DB
DATABASE_URL=postgresql+asyncpg://inspection_user:inspection_pass_change_me@localhost:5432/inspection_db
DATABASE_URL_SYNC=postgresql://inspection_user:inspection_pass_change_me@localhost:5432/inspection_db

# JWT
JWT_SECRET_KEY=please_change_this_to_a_random_64_char_string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# 관리자 API (OTA-002 전용)
ADMIN_API_KEY=admin_dev_key_change_me

# 환경 구분
ENV=dev
```

> ⚠️ **`.gitignore`에 `.env`를 반드시 추가**하세요. 비밀키가 git에 올라가면 안 됩니다.

`backend\.gitignore`:

```
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
postgres_data/
```

---

## 6. SQL 스키마 작성 (`sql/001_schema.sql`)

12 테이블 전체. 명세서 `schema_diagram.md` 그대로입니다.

```sql
-- ============================================
-- 1. pgvector 확장 활성화
-- ============================================
CREATE EXTENSION IF NOT EXISTS vector;

-- ============================================
-- 2. ENUM 타입
-- ============================================
CREATE TYPE 모델_유형_ENUM AS ENUM ('TFLITE', 'PTH');
CREATE TYPE 세션_상태_ENUM AS ENUM ('진행중', '완료');
CREATE TYPE 추론_위치_ENUM AS ENUM ('단말', '서버');
CREATE TYPE 결과_처리_상태_ENUM AS ENUM ('완료', '미완료');

-- ============================================
-- 3. 마스터 테이블
-- ============================================

CREATE TABLE 공정 (
    공정_ID            SERIAL PRIMARY KEY,
    공정_이름          VARCHAR(50) NOT NULL UNIQUE,
    결함_유형_목록     JSONB NOT NULL DEFAULT '[]',
    신뢰도_임계값      FLOAT NOT NULL DEFAULT 0.85,
    서버_재확인_임계값  FLOAT NOT NULL DEFAULT 0.70,
    활성_여부          BOOLEAN NOT NULL DEFAULT false,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE 검사원 (
    검사원_ID          SERIAL PRIMARY KEY,
    이름               VARCHAR(50) NOT NULL,
    부서               VARCHAR(100),
    비밀번호_해시      VARCHAR(255) NOT NULL,
    활성_여부          BOOLEAN NOT NULL DEFAULT true,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    마지막_로그인_일시 TIMESTAMP
);

CREATE TABLE 사용자_설정 (
    설정_ID            SERIAL PRIMARY KEY,
    검사원_ID          INT NOT NULL UNIQUE REFERENCES 검사원(검사원_ID) ON DELETE CASCADE,
    푸시_알림          BOOLEAN NOT NULL DEFAULT true,
    언어               VARCHAR(5) NOT NULL DEFAULT 'ko',
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    수정_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE 검사_구역 (
    탱크_타입          VARCHAR(2) PRIMARY KEY,
    구역_코드          JSONB NOT NULL,
    구역_설명          VARCHAR(200),
    공정_ID            INT NOT NULL REFERENCES 공정(공정_ID)
);

-- ============================================
-- 4. 모델 레지스트리
-- ============================================

-- 단일 통합 모델: 30클래스(불량 19 + 양품 11). `공정_ID`는 메타로만 유지(NULL 허용) — 모델 선택과 무관.
CREATE TABLE 모델_레지스트리 (
    모델_ID            SERIAL PRIMARY KEY,
    공정_ID            INT REFERENCES 공정(공정_ID),  -- NULL 허용 (단일 모델 — 메타로만 유지)
    모델_버전          VARCHAR(20) NOT NULL,
    모델_유형          모델_유형_ENUM NOT NULL,
    파일_경로          VARCHAR(500) NOT NULL,
    파일_해시          VARCHAR(80),
    클래스_라벨        JSONB NOT NULL,
    활성_여부          BOOLEAN NOT NULL DEFAULT false,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_model_active ON 모델_레지스트리(모델_유형, 활성_여부);  -- 단일 모델: 공정_ID 미사용

-- ============================================
-- 5. 트랜잭션 테이블
-- ============================================

CREATE TABLE 검사_세션 (
    세션_ID            SERIAL PRIMARY KEY,
    검사원_ID          INT NOT NULL REFERENCES 검사원(검사원_ID),
    공정_ID            INT NOT NULL REFERENCES 공정(공정_ID),
    탱크_타입          VARCHAR(2) NOT NULL REFERENCES 검사_구역(탱크_타입),
    선택_구역          VARCHAR(50) NOT NULL,
    선택_세부위치      VARCHAR(50) NOT NULL,
    총_이미지_수       INT NOT NULL DEFAULT 0,
    양품_수            INT NOT NULL DEFAULT 0,
    불량_수            INT NOT NULL DEFAULT 0,
    세션_상태          세션_상태_ENUM NOT NULL DEFAULT '진행중',
    시작_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    종료_일시          TIMESTAMP
);

-- 1일 1세션 정책 DB-level enforce
CREATE UNIQUE INDEX uniq_active_session_per_inspector
    ON 검사_세션(검사원_ID) WHERE 세션_상태 = '진행중';

CREATE INDEX idx_session_inspector_date ON 검사_세션(검사원_ID, 시작_일시 DESC);

CREATE TABLE 검사_이미지 (
    이미지_ID          SERIAL PRIMARY KEY,
    세션_ID            INT NOT NULL REFERENCES 검사_세션(세션_ID) ON DELETE CASCADE,
    이미지_경로        VARCHAR(500) NOT NULL,
    촬영_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_image_session ON 검사_이미지(세션_ID);

CREATE TABLE 검사_결과 (
    결과_ID            SERIAL PRIMARY KEY,
    이미지_ID          INT NOT NULL REFERENCES 검사_이미지(이미지_ID) ON DELETE CASCADE,
    공정_ID            INT NOT NULL REFERENCES 공정(공정_ID),
    모델_ID            INT REFERENCES 모델_레지스트리(모델_ID),
    추론_위치          추론_위치_ENUM NOT NULL,
    대표_여부          BOOLEAN NOT NULL DEFAULT false,
    품질_여부          BOOLEAN NOT NULL,
    결함_유형          VARCHAR(50) NOT NULL,
    신뢰도_점수        FLOAT NOT NULL,
    상위_예측          JSONB,
    추론_지연_ms       INT,
    사람_재확인_필요   BOOLEAN NOT NULL DEFAULT false,
    "Grad-CAM_경로"   VARCHAR(500),
    결과_처리_상태     결과_처리_상태_ENUM NOT NULL DEFAULT '미완료',
    시작_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    완료_일시          TIMESTAMP
);

CREATE INDEX idx_result_image ON 검사_결과(이미지_ID);
CREATE INDEX idx_result_representative ON 검사_결과(이미지_ID) WHERE 대표_여부 = true;

CREATE TABLE 검사_피드백 (
    피드백_ID          SERIAL PRIMARY KEY,
    결과_ID            INT NOT NULL UNIQUE REFERENCES 검사_결과(결과_ID) ON DELETE CASCADE,
    검사원_ID          INT NOT NULL REFERENCES 검사원(검사원_ID),
    세션_ID            INT NOT NULL REFERENCES 검사_세션(세션_ID),
    검사원_수정_여부   BOOLEAN NOT NULL DEFAULT false,
    수정된_결함_유형   VARCHAR(50),
    심각도             VARCHAR(20),
    의견               TEXT,
    최종_조치_내용     TEXT,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    수정_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

-- ============================================
-- 6. RAG 테이블
-- ============================================

CREATE TABLE 매뉴얼 (
    매뉴얼_ID          SERIAL PRIMARY KEY,
    공정_ID            INT NOT NULL REFERENCES 공정(공정_ID),
    제목               VARCHAR(200) NOT NULL,
    내용               TEXT NOT NULL,
    내용_벡터          VECTOR(1024),
    출처               VARCHAR(200),
    페이지_번호        INT,
    청크_순서          INT,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_manual_process ON 매뉴얼(공정_ID);
-- 벡터 검색 인덱스 (행 100개 이상 들어간 후 만들기 권장 → 14번 단계에서 추가)

CREATE TABLE 조치_권고 (
    권고_ID            SERIAL PRIMARY KEY,
    결과_ID            INT NOT NULL REFERENCES 검사_결과(결과_ID) ON DELETE CASCADE,
    조치_요약          VARCHAR(500),
    조치_상세          TEXT,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    수정_일시          TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE 조치_권고_매뉴얼 (
    매핑_ID            SERIAL PRIMARY KEY,
    권고_ID            INT NOT NULL REFERENCES 조치_권고(권고_ID) ON DELETE CASCADE,
    매뉴얼_ID          INT NOT NULL REFERENCES 매뉴얼(매뉴얼_ID),
    순위               INT NOT NULL CHECK (순위 BETWEEN 1 AND 3),
    유사도_점수        FLOAT NOT NULL,
    생성_일시          TIMESTAMP NOT NULL DEFAULT NOW(),
    UNIQUE(권고_ID, 순위),
    UNIQUE(권고_ID, 매뉴얼_ID)
);
```

---

## 7. 시드 데이터 (`sql/002_seed.sql`)

테이블은 비어 있으면 API 테스트를 못 합니다. 최소 데이터를 넣습니다.

```sql
-- ============================================
-- 시드 데이터 (개발 편의용)
-- ============================================

-- 공정 6개 (MVP는 표면처리만 활성)
INSERT INTO 공정 (공정_이름, 결함_유형_목록, 신뢰도_임계값, 서버_재확인_임계값, 활성_여부) VALUES
('표면처리','["균열-도장","균열-보온재","도장흐름-도장","도막떨어짐-도장","도막분리-도장","스크래치-모재","스크래치-도장","스크래치-보온재","보온재손상-보온재","탱크클리닝불량-모재","표면양품-모재","표면양품-도장","표면양품-보온재"]', 0.85, 0.70, true),
('용접','["용접불량-조인트","용접블로우홀-조인트","용접양품-조인트"]', 0.85, 0.70, false),
('절단','["절단불량-모재","절단불량-보온재","절단양품-모재","절단양품-보온재"]', 0.85, 0.70, false),
('케이블','["케이블설치불량-케이블그랜드","케이블설치양품-케이블그랜드","케이블손상-케이블","케이블양품-케이블","바인딩불량-케이블타이","바인딩양품-케이블타이"]', 0.85, 0.70, false),
('파이프','["볼트체결불량-파이프","볼트체결양품-파이프"]', 0.85, 0.70, false),
('폼스프레이','["폼스프레이불량-우레탄폼","폼스프레이양품-우레탄폼"]', 0.85, 0.70, false);

-- 검사_구역 (탱크 B/C — 공정_ID는 모두 표면처리=1)
INSERT INTO 검사_구역 (탱크_타입, 구역_코드, 구역_설명, 공정_ID) VALUES
('B', '{"외부": ["지지대"]}', 'B형 탱크 (자립식 각형)', 1),
('C', '{"외벽": ["경판", "바디"], "내부": ["바닥", "격벽"], "외부": ["지지대", "플랫폼"]}', 'C형 탱크 (멤브레인식)', 1);

-- 더미 검사원 1명 (비밀번호: test1234)
-- bcrypt 해시는 Python에서 미리 생성한 값. 패스워드 = "test1234"
INSERT INTO 검사원 (검사원_ID, 이름, 부서, 비밀번호_해시, 활성_여부) VALUES
(1, '김검사', '제4터미널 / 안전운영팀',
 '$2b$12$LfWQpgKZBxRGYxr.HwL5/.gKi3oM7XFiR7eUVe1F6dPAxBSqGfL8.', true);

-- ID 시퀀스 보정 (수동 ID INSERT 후)
SELECT setval('검사원_검사원_ID_seq', (SELECT MAX(검사원_ID) FROM 검사원));

-- 더미 사용자 설정
INSERT INTO 사용자_설정 (검사원_ID, 푸시_알림, 언어) VALUES (1, true, 'ko');

-- 단일 통합 모델 (.pth 1개, .tflite 1개 — 30클래스, 공정_ID=NULL: 단일 모델 = 공정 무관)
INSERT INTO 모델_레지스트리 (공정_ID, 모델_버전, 모델_유형, 파일_경로, 파일_해시, 클래스_라벨, 활성_여부) VALUES
(NULL, 'v1', 'PTH',    'local://models/best_model_v5_datamatch_full.pth',    'sha256:placeholder', '{"0":"균열-도장","1":"균열-보온재","2":"도장흐름-도장","3":"도막떨어짐-도장","4":"도막분리-도장","5":"스크래치-모재","6":"스크래치-도장","7":"스크래치-보온재","8":"보온재손상-보온재","9":"탱크클리닝불량-모재","10":"표면양품-모재","11":"표면양품-도장","12":"표면양품-보온재","13":"용접불량-조인트","14":"용접블로우홀-조인트","15":"용접양품-조인트","16":"절단불량-모재","17":"절단불량-보온재","18":"절단양품-모재","19":"절단양품-보온재","20":"케이블설치불량-케이블그랜드","21":"케이블손상-케이블","22":"바인딩불량-케이블타이","23":"케이블설치양품-케이블그랜드","24":"케이블양품-케이블","25":"바인딩양품-케이블타이","26":"볼트체결불량-파이프","27":"볼트체결양품-파이프","28":"폼스프레이불량-우레탄폼","29":"폼스프레이양품-우레탄폼"}', true),
(NULL, 'v1', 'TFLITE', 'local://models/best_model_v5_datamatch_full.tflite', 'sha256:placeholder', '{"0":"균열-도장","1":"균열-보온재","2":"도장흐름-도장","3":"도막떨어짐-도장","4":"도막분리-도장","5":"스크래치-모재","6":"스크래치-도장","7":"스크래치-보온재","8":"보온재손상-보온재","9":"탱크클리닝불량-모재","10":"표면양품-모재","11":"표면양품-도장","12":"표면양품-보온재","13":"용접불량-조인트","14":"용접블로우홀-조인트","15":"용접양품-조인트","16":"절단불량-모재","17":"절단불량-보온재","18":"절단양품-모재","19":"절단양품-보온재","20":"케이블설치불량-케이블그랜드","21":"케이블손상-케이블","22":"바인딩불량-케이블타이","23":"케이블설치양품-케이블그랜드","24":"케이블양품-케이블","25":"바인딩양품-케이블타이","26":"볼트체결불량-파이프","27":"볼트체결양품-파이프","28":"폼스프레이불량-우레탄폼","29":"폼스프레이양품-우레탄폼"}', true);
```

> ⚠️ **비밀번호 해시는 진짜 bcrypt로 다시 생성**해야 합니다. 위 해시는 형식 예시. 05번 단계에서 진짜 해시로 갱신합니다.

---

## 8. 컨테이너 띄우기 & 테이블 생성 확인

```powershell
cd C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
docker compose up -d
```

10초 정도 기다린 후:

```powershell
docker compose ps
```

`inspection_postgres ... healthy` 가 보이면 성공.

DB 안에 진짜로 테이블이 만들어졌는지 확인:

```powershell
docker exec -it inspection_postgres psql -U inspection_user -d inspection_db -c "\dt"
```

12개 테이블이 나열되면 성공:
```
                List of relations
 Schema |        Name        | Type  |     Owner
--------+--------------------+-------+----------------
 public | 공정               | table | inspection_user
 public | 검사원             | table | inspection_user
 ... (12개)
```

pgvector도 확인:
```powershell
docker exec -it inspection_postgres psql -U inspection_user -d inspection_db -c "SELECT extname, extversion FROM pg_extension WHERE extname='vector';"
```
`vector | 0.x.x` 같이 나오면 성공.

> 💡 **컨테이너 재시작 시 테이블이 또 생성되지 않나요?** `docker-entrypoint-initdb.d`의 SQL은 **데이터 디렉터리가 비어 있을 때만** 실행됩니다. 한 번 만들어지면 `postgres_data` 볼륨이 남아 있어서 재실행 안 됩니다.
> 만약 스키마를 갈아엎고 처음부터 다시 하려면:
> ```powershell
> docker compose down -v   # -v 옵션이 볼륨까지 삭제
> docker compose up -d
> ```

---

## 9. FastAPI 뼈대 코드

### 9-1. `app/core/config.py`

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    DATABASE_URL: str
    DATABASE_URL_SYNC: str
    JWT_SECRET_KEY: str
    JWT_ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    REFRESH_TOKEN_EXPIRE_DAYS: int = 7
    ADMIN_API_KEY: str
    ENV: str = "dev"


settings = Settings()
```

### 9-2. `app/core/db.py`

```python
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine, AsyncSession
from app.core.config import settings

engine = create_async_engine(settings.DATABASE_URL, echo=False, pool_pre_ping=True)
AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)


async def get_db() -> AsyncSession:
    async with AsyncSessionLocal() as session:
        yield session
```

### 9-3. `app/main.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from sqlalchemy import text
from app.core.db import engine

app = FastAPI(
    title="선박 LNG탱크 부품 품질 검사 API",
    version="0.1.0",
    docs_url="/api/docs",
    redoc_url="/api/redoc",
    openapi_url="/api/openapi.json",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # dev 전용. 운영에서는 사내 도메인만
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/api/health")
async def health_check():
    """헬스 체크. DB 연결까지 검증."""
    async with engine.connect() as conn:
        result = await conn.execute(text("SELECT 1"))
        result.scalar_one()
    return {"status": "ok", "db": "ok"}


# 라우터는 다음 단계부터 점진적으로 추가
# from app.api import auth, tank, session, inspect, result, settings as settings_api
# app.include_router(auth.router, prefix="/api/auth", tags=["auth"])
```

> 💡 **CORS가 뭐죠?** 브라우저가 다른 도메인의 API를 호출할 때 보안 정책. Flutter 앱은 브라우저가 아니지만, 추후 디버깅 용으로 웹에서도 호출할 수 있으니 켜둠.

---

## 10. 서버 실행 & 헬스체크

가상환경이 활성화된 상태에서:

```powershell
cd C:\Users\yejin\Desktop\sesac_final\UsersyejinDesktopSeSAC_final\backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

다음 메시지가 보이면 성공:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [...]
INFO:     Started server process [...]
INFO:     Application startup complete.
```

브라우저에서:
- http://localhost:8000/api/health → `{"status":"ok","db":"ok"}` 가 보여야 함
- http://localhost:8000/api/docs → Swagger UI (자동 생성된 API 문서) 페이지가 보여야 함

> 💡 **Swagger UI**는 API 명세를 자동 생성해주는 도구입니다. 라우터를 추가할 때마다 여기에 자동으로 나타나서 브라우저에서 API를 호출해볼 수 있습니다.

---

## 자주 발생하는 오류와 해결

### `docker compose up -d` → "port 5432 already in use"
- 다른 PostgreSQL이 5432번을 쓰는 중. 그 PostgreSQL을 끄거나, `docker-compose.yml`의 포트를 `5433:5432`로 바꾸고 `.env`의 `DATABASE_URL`도 5433으로

### `uvicorn` 명령을 찾을 수 없음
- 가상환경 활성화를 안 한 상태. `.\.venv\Scripts\Activate.ps1` 먼저

### `asyncpg.exceptions.InvalidPasswordError`
- `.env`의 비밀번호와 `docker-compose.yml`의 `POSTGRES_PASSWORD`가 일치하지 않음

### 한글 컬럼/테이블 이름 때문에 에러
- SQLAlchemy 모델을 작성할 때 `__tablename__ = '"검사원"'` 처럼 백슬래시 따옴표를 쓰거나, 영문 alias를 만들어야 합니다. 다음 단계에서 처리.

### SQL 첫 실행이 안 됨 (시드가 없음)
- `postgres_data` 볼륨이 이미 존재해서 init 스크립트가 skip된 상태. `docker compose down -v` 후 다시 `up -d`

---

## ✅ 다음 단계로 가기 전 체크리스트

- [ ] `docker compose ps`에서 `inspection_postgres`가 `healthy` 상태
- [ ] `docker exec ... psql ... \dt` 결과에 12개 테이블이 모두 보인다
- [ ] pgvector 확장이 활성화되어 있다
- [ ] http://localhost:8000/api/health 가 `{"status":"ok","db":"ok"}`를 반환한다
- [ ] http://localhost:8000/api/docs 가 Swagger UI를 보여준다
- [ ] `검사원` 테이블에 더미 검사원 1명이 들어 있다 (`SELECT * FROM 검사원;`)

> 💡 **앞으로의 작업 흐름**:
> 1. PowerShell 창 1: `docker compose up -d` 로 DB 실행 (한 번만)
> 2. PowerShell 창 2: 가상환경 활성화 + `uvicorn app.main:app --reload` (백엔드 개발 중에는 계속)
> 3. PowerShell 창 3: Flutter 개발용

다음 단계 **[03_Flutter_프로젝트_초기화.md](03_Flutter_프로젝트_초기화.md)** 로 이동하세요.
